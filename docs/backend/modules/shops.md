# Модуль магазинов (Shops)

Модуль shops управляет магазинами розничной сети. Магазины могут принадлежать к региону, иметь внешний ID из контура заказчика и хранить состояние готовности цифрового маршрута.

## Эндпоинты

- `POST /api/v1/shops` создаёт магазин.
- `GET /api/v1/shops` возвращает список активных магазинов с пагинацией.
- `GET /api/v1/shops/:id` возвращает информацию об одном магазине.
- `POST /api/v1/shops/:id/route-setup/start` начинает настройку маршрута и создаёт каркас точек `1..N`.
- `GET /api/v1/shops/:id/route-setup` возвращает состояние настройки маршрута, список точек и следующий номер для привязки.
- `POST /api/v1/shops/:id/route-setup/points/:sortOrder/bind-nfc` привязывает UID NFC-метки к номеру точки маршрута.
- `POST /api/v1/shops/:id/route-setup/reset` отменяет текущую настройку цифрового маршрута и возвращает магазин в `not_configured`.

## Бизнес-правила

- Магазины поддерживают мягкое удаление через `deleted_at`.
- Запросы активных магазинов возвращают только записи с `is_active = true`.
- Если часовой пояс не указан, backend использует значение по умолчанию `Europe/Moscow`, заданное в базе данных.
- Ссылки на регион необязательны и используют `ON DELETE SET NULL`.
- `external_id` хранит ID магазина из операционного контура заказчика и уникален, если указан.
- Маршрут магазина имеет статусы `not_configured`, `setup_in_progress`, `ready`.
- Начать обход можно только по магазину со статусом маршрута `ready`.
- Настройка маршрута создаёт точки с порядковыми номерами. До привязки NFC UID такие точки не считаются активными для боевого обхода.
- При привязке UID система проверяет, что активная метка не назначена другой контрольной точке.
- Сброс настройки маршрута деактивирует точки маршрута магазина, отвязывает от них NFC и деактивирует прежние NFC-метки. Строки точек не удаляются, чтобы история уже выполненных обходов продолжала ссылаться на прежние точки.
- После сброса можно снова вызвать `route-setup/start` и пройти регистрацию NFC заново.
