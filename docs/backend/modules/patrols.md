# Модуль Patrols

Модуль `patrols` управляет конкретными обходами сотрудников и событиями NFC-сканирования.

## Эндпоинты

- `POST /api/v1/patrols` — стартует обход.
- `GET /api/v1/patrols/shop/:shopId` — возвращает обходы магазина с пагинацией.
- `GET /api/v1/patrols/employee/:employeeId` — возвращает историю конкретного обходчика с пагинацией; роли `admin`, `inspector`.
- `GET /api/v1/patrols/incidents` — возвращает список инцидентов обходов с фильтрами.
- `GET /api/v1/patrols/:id` — возвращает обход с событиями.
- `POST /api/v1/patrols/:id/events` — записывает событие сканирования NFC.
- `POST /api/v1/patrols/:id/complete` — завершает обход и сохраняет отчет сотрудника.
- `POST /api/v1/patrols/:id/cancel` — отменяет активный обход с причиной отмены.

## Доступ

Все endpoints обычного модуля `patrols` требуют `Authorization: Bearer <accessToken>`.

- `POST /api/v1/patrols`, `POST /api/v1/patrols/:id/events`, `POST /api/v1/patrols/:id/complete`, `POST /api/v1/patrols/:id/cancel` доступны только роли `admin`. Сценарии сотрудника в мобильном приложении должны использовать защищенные endpoints `/api/v1/mobile/patrols/*`.
- `GET /api/v1/patrols/shop/:shopId`, `GET /api/v1/patrols/incidents`, `GET /api/v1/patrols/:id`, `GET /api/v1/patrols/employee/:employeeId` доступны ролям `admin`, `inspector`.
- Для роли `inspector` доступ к данным обходов дополнительно ограничен назначенными магазинами пользователя.

Списки поддерживают серверные фильтры и сортировку:

- `GET /api/v1/patrols/shop/:shopId` и `GET /api/v1/patrols/employee/:employeeId`: `page`, `limit`, `status`, `from`, `to`, `sort`. Сортировки: `createdAt:desc`, `createdAt:asc`, `startedAt:desc`, `startedAt:asc`, `status:asc`, `status:desc`.
- `GET /api/v1/patrols/incidents`: `page`, `limit`, `shopId`, `employeeId`, `type`, `from`, `to`, `search`, `sort`. Поиск идет по сообщению инцидента, ФИО сотрудника и названию магазина; сортировки: `createdAt:desc`, `createdAt:asc`, `type:asc`, `type:desc`.

## Бизнес-правила

- Обход нельзя стартовать, если в магазине нет активных контрольных точек.
- Если при старте указан `routeId` или расписание привязано к маршруту, обход использует точки этого маршрута, а не все точки магазина.
- NFC-событие для обхода с маршрутом принимается только по точке, включённой в этот маршрут.
- Обходчик должен быть назначен на магазин через `user_shop_assignments`.
- Обход нельзя стартовать, пока маршрут магазина не находится в статусе `ready`.
- Для планового обхода `scheduleId` должен указывать на активное расписание того же магазина, доступное в текущий день и локальное время магазина с учетом `early_start_minutes`.
- Повторный обход по тому же `scheduleId` и тому же рассчитанному `dueAt` запрещен, если предыдущий обход не отменен. Это защищает от дублей после авто-завершения, но сохраняет сценарий "отменить и начать заново".
- Дедлайн планового обхода backend рассчитывает из `end_time` расписания; клиент не может переопределить его через `dueAt`.
- Фоновая задача раз в минуту переводит незавершённый обход с истёкшим `due_at` в `overdue`.
- Просроченный обход остаётся активным для сотрудника: NFC-события принимаются, а после сканирования всех точек статус становится `completed`.
- Без `scheduleId` создаётся внеплановый обход без серверного дедлайна, если `dueAt` явно не передан административным API.
- При старте `total_points` фиксируется как количество активных точек магазина.
- При старте с `routeId` `total_points` фиксируется как количество активных точек выбранного маршрута.
- События принимаются для обхода в статусе `in_progress` или `overdue`.
- NFC UID нормализуется в нижний регистр.
- NFC-метка должна быть активной и соответствовать контрольной точке.
- Контрольная точка должна принадлежать тому же магазину, что и обход.
- После каждого события увеличивается `scanned_points`.
- Когда `scanned_points >= total_points`, обход автоматически переводится в `completed`.
- Ручное завершение разрешено только если все точки уже отсканированы.
- `complete` принимает опциональное поле `completionReport`. Его нужно использовать для диалогового отчета сотрудника при завершении обхода: например, чтобы объяснить большой тайминг из-за покупателя, руководства или помощи другому сотруднику.
- Если последний NFC-скан уже автоматически перевел обход в `completed`, повторный вызов `complete` не падает и может дозаписать `completionReport`.
- `cancel` разрешен для статусов `pending`, `in_progress`, `overdue` и переводит обход в `cancelled`. В `cancellationReason` сохраняется причина отмены, после чего сотрудник может начать новый обход.
- После отмены online-события в этот обход больше не принимаются, но offline sync с `localId` сохраняет поздние события как `late_sync`.
- Первый завершённый обход магазина в корректном порядке маршрута формирует эталонные интервалы `patrol_route_intervals`.
- Для MVP допустимый коридор интервала: от 50% до 200% эталонного времени.
- Если фактический интервал меньше нижнего порога, создаётся инцидент `short_interval`.
- Если фактический интервал больше верхнего порога, создаётся инцидент `long_interval`.
- Если backend принимает NFC-событие с перескоком через одну или несколько точек, создаётся инцидент `missed_point`.
- Если mobile-клиент блокирует скан не по порядку, он вызывает `POST /api/v1/mobile/patrols/:id/missed-point-attempts`; backend создает инцидент `missed_point` без создания `patrol_event`.
- Инциденты по таймингам не блокируют запись NFC-события: событие сохраняется, а подозрение фиксируется отдельно в `patrol_incidents`.
- Инцидент `missed_point`, созданный через `missed-point-attempts`, фиксирует только намерение пропустить точку: прогресс обхода не меняется, `scanned_points` не увеличивается.

## Тайминги и инциденты

Эталон больше не строится по первому завершенному обходу. Backend рассчитывает норматив маршрута автоматически по завершенным обходам конкретного `routeId` за последние 14 дней. В расчет входят только полные обходы с корректными `arrive` и `depart` по всем точкам маршрута.

Если выборка меньше минимального порога, норматив считается недостаточно надежным и триггеры `route_too_fast`, `route_suspiciously_fast` и `route_too_slow` не применяются автоматически. Подробный контракт описан в [Route Timing And Point Visits](route-timing-and-point-visits.md).

Инциденты по таймингу привязываются к магазину, обходу, текущему событию и паре точек. Это позволяет веб-панели показывать список подозрительных обходов без пересчёта истории на лету.

## Попытка пропуска точки

`recordMissedPointAttempt` используется мобильным сценарием строгого порядка маршрута. Клиент знает текущую ожидаемую точку и, если сотрудник сканирует более позднюю точку, не создает событие обхода. Вместо события клиент отправляет:

- ожидаемую точку (`expectedPatrolPointId`);
- фактически отсканированную точку (`attemptedPatrolPointId`);
- UUID локальной записи (`clientLocalId`);
- UID метки, время скана и `deviceId`.

Backend проверяет:

- обход существует и находится в `in_progress` или `overdue`;
- обход принадлежит текущему сотруднику через mobile endpoint;
- обе точки принадлежат магазину обхода;
- `attemptedPoint.sortOrder > expectedPoint.sortOrder`.

Если условия выполнены, создается `patrol_incident` с типом `missed_point`, `fromPatrolPointId = expectedPatrolPointId`, `toPatrolPointId = attemptedPatrolPointId`. `patrol_event` не создается, поэтому сотрудник должен вернуться к ожидаемой точке и продолжить маршрут в правильном порядке.

Повторная отправка того же `clientLocalId` в рамках обхода идемпотентна: backend возвращает успех
без второго инцидента и уведомления. Уникальность обеспечивается ограничением БД.

После создания инцидента вызывается общий `notifyIncidentCreated`, поэтому push получают проверяющие, назначенные на магазин.

## Просмотр инцидентов

`GET /api/v1/patrols/incidents` используется экраном проверяющего для просмотра подозрительных обходов.

Поддерживаемые фильтры:

- `shopId` — показать инциденты конкретного магазина.
- `employeeId` — показать инциденты конкретного сотрудника.
- `type` — `short_interval`, `long_interval`, `missed_point`.
- `from` / `to` — период по времени создания инцидента.
- `page` / `limit` — пагинация.

Ответ содержит `items`, `total`, `page`, `limit`. В `items` возвращаются связи с обходом, магазином, сотрудником, событием и точками маршрута, чтобы web-панель могла сразу показать контекст нарушения.

## Связь с расписанием

`patrols.schedule_id` связывает конкретный обход с шаблоном `patrol_schedules`. При удалении магазина расписания удаляются каскадно; при деактивации расписания уже созданные обходы и их история сохраняются.

Полный контракт управления окнами описан в [модуле Patrol Schedules](patrol-schedules.md).

## История обходчика

```http
GET /api/v1/patrols/employee/:employeeId?page=1&limit=20
Authorization: Bearer <accessToken>
```

Администратор получает обходы сотрудника по всем магазинам. Проверяющий получает только обходы из назначенных ему магазинов. Ответ имеет стандартную форму `items`, `total`, `page`, `limit`; элементы содержат магазин, сотрудника и расписание.

## Offline sync

Онлайн-запись события через `POST /api/v1/patrols/:id/events` остаётся строгой: обход должен быть `in_progress`, точка и NFC-метка должны быть активны и соответствовать друг другу.

Мобильная синхронизация через `POST /api/v1/mobile/patrols/:id/events/sync` работает мягче, потому что событие уже произошло на устройстве офлайн. Backend:

- не создаёт дубль при повторной отправке того же `localId`;
- не создаёт дубль при повторной отправке события по уже отсканированной точке обхода;
- сохраняет поздние события с флагом `late_sync = true`, не меняя `scanned_points`;
- сохраняет события по деактивированной после скана точке с флагом `point_deactivated_after_scan = true`;
- возвращает по каждому событию статус `created`, `duplicate`, `late_sync` или `point_deactivated`.
