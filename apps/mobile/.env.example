# Публичный HTTPS-адрес API без завершающего слеша. Это не секрет.
EXPO_PUBLIC_API_BASE_URL=https://patrol.example.ru/api/v1

# GOOGLE_SERVICES_JSON задаётся в EAS как file variable.
# Для локальной сборки используется apps/mobile/google-services.json.
